from translate import Translator
translator = Translator(to_lang = 'French')
translation = translator.translate("Failure doesn't mean you are a failure it just means you haven't succeeded yet.")
print(translation)
